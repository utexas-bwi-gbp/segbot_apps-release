Name:           ros-hydro-segbot-navigation
Version:        0.3.0
Release:        0%{?dist}
Summary:        ROS segbot_navigation package

Group:          Development/Libraries
License:        BSD
URL:            http://ros.org/wiki/segbot_navigation
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-hydro-amcl
Requires:       ros-hydro-bwi-interruptable-action-server
Requires:       ros-hydro-eband-local-planner
Requires:       ros-hydro-geometry-msgs
Requires:       ros-hydro-gmapping
Requires:       ros-hydro-map-server
Requires:       ros-hydro-move-base
Requires:       ros-hydro-move-base-msgs
Requires:       ros-hydro-nav-msgs
Requires:       ros-hydro-navfn
Requires:       ros-hydro-rviz
Requires:       ros-hydro-segbot-bringup
Requires:       ros-hydro-std-srvs
BuildRequires:  ros-hydro-bwi-interruptable-action-server
BuildRequires:  ros-hydro-catkin
BuildRequires:  ros-hydro-geometry-msgs
BuildRequires:  ros-hydro-move-base-msgs
BuildRequires:  ros-hydro-nav-msgs
BuildRequires:  ros-hydro-roslaunch
BuildRequires:  ros-hydro-std-srvs

%description
Contains launch files for running the ROS navigation stack on the segbot using
the eband_local_planner approach, as well as launch files for amcl and gmapping.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/hydro" \
        -DCMAKE_PREFIX_PATH="/opt/ros/hydro" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/hydro

%changelog
* Tue Mar 24 2015 Piyush Khandelwal <piyushk@gmail.com> - 0.3.0-0
- Autogenerated by Bloom

